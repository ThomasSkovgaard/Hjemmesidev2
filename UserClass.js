// Making a class for userInfo in a separate JS-file, and constructing a user with username and password
class userInfo{
    constructor(username, password){
        this.username = username;
        this.password = password;
    }
}